<?xml version="1.0" encoding="UTF-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android">

  <RelativeLayout android:id="@+id/feed_header"
    android:layout_width="fill_parent"
    android:layout_height="50dp"
    android:gravity="center_vertical">

    <TextView android:id="@+id/feed_title"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_marginStart="8dp"
      android:layout_toLeftOf="@id/feed_progress"
      android:layout_alignParentLeft="true"
      android:ellipsize="end"
      android:maxLines="1"
      android:textColor="?attr/colorPrimary"
      android:text="@string/catalog_unknown_feed" />

    <TextView android:id="@+id/feed_more"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_alignParentEnd="true"
      android:layout_marginStart="8dp"
      android:layout_marginTop="8dp"
      android:layout_marginEnd="4dp"
      android:ellipsize="end"
      android:maxLines="1"
      android:textColor="?attr/colorPrimary"
      android:text="@string/catalog_more_feed"
      android:textSize="12dp" />

    <ProgressBar android:id="@+id/feed_progress"
      style="?android:attr/progressBarStyle"
      android:layout_width="wrap_content"
      android:layout_height="25dp"
      android:layout_alignParentEnd="true"
      android:layout_marginStart="8dp"
      android:layout_marginEnd="34dp" />
  </RelativeLayout>

  <HorizontalScrollView android:id="@+id/feed_scroller"
    android:layout_width="match_parent"
    android:layout_height="110dp"
    android:fillViewport="true">
    <LinearLayout android:id="@+id/feed_scroller_contents"
      android:layout_width="wrap_content"
      android:layout_height="match_parent"
      android:orientation="horizontal"
      android:paddingStart="8dp"
      android:paddingEnd="8dp" />
  </HorizontalScrollView>

  <!-- Invisible padding element -->

  <View android:layout_width="match_parent"
    android:layout_height="8dp" />

</merge>