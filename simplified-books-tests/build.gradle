apply plugin: 'com.android.library'

android {
  compileSdkVersion 25
  buildToolsVersion "26.0.0"
}

description = 'simplified-books-tests'

dependencies {
  compile project(':simplified-books-core')
  compile project(':simplified-test-utilities')
}
