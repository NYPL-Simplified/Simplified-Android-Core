apply plugin: 'com.android.library'

android {
  compileSdkVersion 26
  buildToolsVersion "26.0.2"
}

description = 'simplified-files-tests'

dependencies {
  compile project(':simplified-files')
  compile project(':simplified-test-utilities')
}
