apply plugin: 'com.android.library'

android {
  compileSdkVersion 26
  buildToolsVersion "26.0.2"
}

description = 'simplified-http-tests'

dependencies {
  compile project(':simplified-http-core')
  compile project(':simplified-test-utilities')
}
