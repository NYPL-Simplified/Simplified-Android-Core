apply plugin: 'com.android.library'

android {
  compileSdkVersion 26
  buildToolsVersion "26.0.2"
}

description = 'simplified-json-tests'

dependencies {
  compile project(':simplified-json-core')
  compile project(':simplified-test-utilities')
}
