apply plugin: 'com.android.library'

android {
  compileSdkVersion 26
  buildToolsVersion "26.0.2"
}

description = 'simplified-opds-tests'

dependencies {
  compile project(':simplified-opds-core')
  compile project(':simplified-test-utilities')
}
